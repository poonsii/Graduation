// stored inside SavedPlantState.earnedBadgeIds - string ids instead of an enum so old saves keep working even if badges get reordered.
public static class CalendarBadgeIds
{
    public const string PlantPlanner = "plant_planner";
    public const string WeekStreak = "week_streak";
}
